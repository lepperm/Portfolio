/**
 * @fileoverview Story for ListBlock component.
 */

import React from "react";
import { ListBlock } from "./ListBlock";

export default {
  title: "ListBlock",
  component: ListBlock,
};

export const Basic = () => <ListBlock />;
