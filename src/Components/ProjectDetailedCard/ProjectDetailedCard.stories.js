/**
 * @fileoverview Story for ProjectDetailedCard component.
 */

import React from "react";
import { ProjectDetailedCard } from "./ProjectDetailedCard";

export default {
  title: "ProjectDetailedCard",
  component: ProjectDetailedCard,
};

export const Basic = () => <ProjectDetailedCard />;
