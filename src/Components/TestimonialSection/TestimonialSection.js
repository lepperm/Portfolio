/**
 * @fileoverview TestimonialSection component.
 */

import React from "react";
import PropTypes from "prop-types";

/**
 * TestimonialSection component
 *
 * @return {Component} TestimonialSection component
 */
export const TestimonialSection = () => {
  return (
    <section>
      Does it even make sense to have a testimonials section for myself? 🤔
    </section>
  );
};

/**
 * Prop types for TestimonialSection component
 */
TestimonialSection.propTypes = {
  //
};
