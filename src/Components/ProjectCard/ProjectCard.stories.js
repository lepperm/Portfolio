/**
 * @fileoverview Story for ProjectCard component.
 */

import React from "react";
import { ProjectCard } from "./ProjectCard";

export default {
  title: "ProjectCard",
  component: ProjectCard,
};

export const Basic = () => <ProjectCard />;
